#pragma once
#include <vector>

int minSubArrayLenNative(const std::vector<int>& arr, int target);
int minSubArrayLenSliding(const std::vector<int>& arr, int target);
std::vector<int> MakeArray(int size);